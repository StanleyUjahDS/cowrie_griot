import 'package:flutter/material.dart';

class AppColors {
  // ============================================================
  // GRIOT
  // DEFAULT BRAND THEME
  // Deep sea-green + gold
  // ============================================================

  static const Color griotPrimary = Color(0xFFD4A84F);
  static const Color griotPrimaryDark = Color(0xFF9A6F24);
  static const Color griotPrimarySoft = Color(0xFFE8D6AD);

  static const Color griotDarkBackground = Color(0xFF00211A);
  static const Color griotDarkSurface = Color(0xFF0D231D);
  static const Color griotDarkVariant = Color(0xFF16332B);

  static const Color griotLightBackground = Color(0xFFFFFFFF);
  static const Color griotLightSurface = Color(0xFFFCFCFC);
  static const Color griotLightVariant = Color(0xFFF4F5F4);

  // ============================================================
  // MESSENGER / OCEAN
  // Facebook / Messenger inspired
  // ============================================================

  static const Color messengerPrimary = Color(0xFF1877F2);
  static const Color messengerPrimaryDark = Color(0xFF0D5AC7);
  static const Color messengerPrimarySoft = Color(0xFFDCEBFF);

  static const Color messengerDarkBackground = Color(0xFF18191A);
  static const Color messengerDarkSurface = Color(0xFF242526);
  static const Color messengerDarkVariant = Color(0xFF3A3B3C);

  static const Color messengerLightBackground = Color(0xFFF0F2F5);
  static const Color messengerLightSurface = Color(0xFFFFFFFF);
  static const Color messengerLightVariant = Color(0xFFE4E6EB);

  // ============================================================
  // WHATSAPP / EMERALD
  // ============================================================

  static const Color whatsappPrimary = Color(0xFF25D366);
  static const Color whatsappPrimaryDark = Color(0xFF1DA851);
  static const Color whatsappPrimarySoft = Color(0xFFDDF8E7);

  static const Color whatsappDarkBackground = Color(0xFF111B21);
  static const Color whatsappDarkSurface = Color(0xFF202C33);
  static const Color whatsappDarkVariant = Color(0xFF2A3942);

  static const Color whatsappLightBackground = Color(0xFFEFEAE2);
  static const Color whatsappLightSurface = Color(0xFFFFFFFF);
  static const Color whatsappLightVariant = Color(0xFFF5F5F5);

  // ============================================================
  // VIOLET
  // ============================================================

  static const Color violetPrimary = Color(0xFF8B6CFF);
  static const Color violetPrimaryDark = Color(0xFF6548D8);
  static const Color violetPrimarySoft = Color(0xFFE9E3FF);

  // ============================================================
  // LAVENDER
  // ============================================================

  static const Color lavenderPrimary = Color(0xFFB59CFF);
  static const Color lavenderPrimaryDark = Color(0xFF8064D8);
  static const Color lavenderPrimarySoft = Color(0xFFF0EAFF);

  // ============================================================
  // ROSE
  // Feminine pink / rose
  // ============================================================

  static const Color rosePrimary = Color(0xFFFF4F87);
  static const Color rosePrimaryDark = Color(0xFFD92F68);
  static const Color rosePrimarySoft = Color(0xFFFFDDE9);

  static const Color roseDarkBackground = Color(0xFF0D070A);
  static const Color roseDarkSurface = Color(0xFF160C11);
  static const Color roseDarkVariant = Color(0xFF24121A);

  static const Color roseLightBackground = Color(0xFFFFF8FB);
  static const Color roseLightSurface = Color(0xFFFFFFFF);
  static const Color roseLightVariant = Color(0xFFFCEBF2);

  // ============================================================
  // GOLD
  // Premium gold
  // ============================================================

  static const Color goldPrimary = Color(0xFFE2B85B);
  static const Color goldPrimaryDark = Color(0xFF9A6F24);
  static const Color goldPrimarySoft = Color(0xFFFFE8AD);

  static const Color goldDarkBackground = Color(0xFF0B0A07);
  static const Color goldDarkSurface = Color(0xFF15120C);
  static const Color goldDarkVariant = Color(0xFF211B0F);

  static const Color goldLightBackground = Color(0xFFFFFCF5);
  static const Color goldLightSurface = Color(0xFFFFFFFF);
  static const Color goldLightVariant = Color(0xFFF8F0D9);

  // ============================================================
  // MIDNIGHT
  // ============================================================

  static const Color midnightPrimary = Color(0xFF6D8CFF);
  static const Color midnightPrimaryDark = Color(0xFF5069D8);
  static const Color midnightPrimarySoft = Color(0xFFE1E7FF);

  // ============================================================
  // SLATE
  // ============================================================

  static const Color slatePrimary = Color(0xFFB0B7C3);
  static const Color slatePrimaryDark = Color(0xFF59636F);
  static const Color slatePrimarySoft = Color(0xFFE5E7EB);

  // ============================================================
  // COMMON
  // ============================================================

  static const Color error = Color(0xFFEF4444);

  static const Color success = Color(0xFF22C55E);

  static const Color green = Color(0xFF00C896);

  static const Color earth = Color(0xFF6A682B);

  // ============================================================
  // DARK TEXT
  // ============================================================

  static const Color darkTextPrimary = Color(0xFFF5F5F5);

  static const Color darkTextSecondary = Color(0xFFD1D1D1);

  static const Color darkTextMuted = Color(0xFF8E8E8E);

  // ============================================================
  // LIGHT TEXT
  // ============================================================

  static const Color lightTextPrimary = Color(0xFF111111);

  static const Color lightTextSecondary = Color(0xFF4B5563);

  static const Color lightTextMuted = Color(0xFF73777A);

  // ============================================================
  // BORDERS
  // ============================================================

  static const Color darkBorder = Color(0xFF303030);

  static const Color lightBorder = Color(0xFFE5E7EB);
}