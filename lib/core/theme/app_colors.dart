import 'package:flutter/material.dart';

/// ============================================================
/// GRIOT APP COLOR SYSTEM
/// ============================================================
///
/// Each theme contains:
///
/// Primary       -> main accent / brand colour
/// PrimaryDark   -> pressed / active / darker accent
/// PrimarySoft   -> subtle accent tint
///
/// DarkBackground -> main dark canvas
/// DarkSurface    -> elevated dark surface
/// DarkVariant    -> secondary dark surface / bubble / field
///
/// LightBackground -> main light canvas
/// LightSurface    -> elevated light surface
/// LightVariant    -> secondary light surface / bubble / field
///
/// The background and surface colours are intentionally chosen
/// to harmonize with each accent rather than simply being darker
/// versions of the accent.
///
/// Griot colours are preserved exactly.
/// ============================================================

class AppColors {
  // ============================================================
  // GRIOT
  // DEFAULT BRAND THEME
  // Deep sea-green + gold
  // ============================================================

  static const Color griotPrimary = Color(0xFFD4A84F);
  static const Color griotPrimaryDark = Color(0xFF9A6F24);
  static const Color griotPrimarySoft = Color(0xFFE8D6AD);

  static const Color griotDarkBackground = Color(0xFF00211A);
  static const Color griotDarkSurface = Color(0xFF0D231D);
  static const Color griotDarkVariant = Color(0xFF16332B);

  static const Color griotLightBackground = Color(0xFFFFFFFF);
  static const Color griotLightSurface = Color(0xFFFCFCFC);
  static const Color griotLightVariant = Color(0xFFF4F5F4);

  // ============================================================
  // MESSENGER
  // Electric Messenger Blue
  // ============================================================

  static const Color messengerPrimary = Color(0xFF0084FF);
  static const Color messengerPrimaryDark = Color(0xFF0064D2);
  static const Color messengerPrimarySoft = Color(0xFFE3F1FF);

  static const Color messengerDarkBackground = Color(0xFF0E1117);
  static const Color messengerDarkSurface = Color(0xFF181C24);
  static const Color messengerDarkVariant = Color(0xFF232936);

  static const Color messengerLightBackground = Color(0xFFF4F6F9);
  static const Color messengerLightSurface = Color(0xFFFFFFFF);
  static const Color messengerLightVariant = Color(0xFFE8EDF3);

  // ============================================================
  // WHATSAPP
  // WhatsApp Green
  // ============================================================

  static const Color whatsappPrimary = Color(0xFF25D366);
  static const Color whatsappPrimaryDark = Color(0xFF075E54);
  static const Color whatsappPrimarySoft = Color(0xFFD9FDD3);

  static const Color whatsappDarkBackground = Color(0xFF0B141A);
  static const Color whatsappDarkSurface = Color(0xFF111B21);
  static const Color whatsappDarkVariant = Color(0xFF202C33);

  static const Color whatsappLightBackground = Color(0xFFEFEAE2);
  static const Color whatsappLightSurface = Color(0xFFFFFFFF);
  static const Color whatsappLightVariant = Color(0xFFF0F2F5);

  static const Color whatsappBlueTicks = Color(0xFF53BDEB);

  // ============================================================
  // VIOLET
  // Rich Amethyst
  // ============================================================

  static const Color violetPrimary = Color(0xFF8B5CF6);
  static const Color violetPrimaryDark = Color(0xFF6D28D9);
  static const Color violetPrimarySoft = Color(0xFFEDE9FE);

  static const Color violetDarkBackground = Color(0xFF100D18);
  static const Color violetDarkSurface = Color(0xFF191526);
  static const Color violetDarkVariant = Color(0xFF27213A);

  static const Color violetLightBackground = Color(0xFFF8F6FC);
  static const Color violetLightSurface = Color(0xFFFFFFFF);
  static const Color violetLightVariant = Color(0xFFF0ECF8);

  // ============================================================
  // LAVENDER
  // Soft Lilac
  // ============================================================

  static const Color lavenderPrimary = Color(0xFFA78BFA);
  static const Color lavenderPrimaryDark = Color(0xFF7C3AED);
  static const Color lavenderPrimarySoft = Color(0xFFF3E8FF);

  static const Color lavenderDarkBackground = Color(0xFF13101B);
  static const Color lavenderDarkSurface = Color(0xFF1D1827);
  static const Color lavenderDarkVariant = Color(0xFF2A2337);

  static const Color lavenderLightBackground = Color(0xFFFAF8FD);
  static const Color lavenderLightSurface = Color(0xFFFFFFFF);
  static const Color lavenderLightVariant = Color(0xFFF1ECF8);

  // ============================================================
  // ROSE
  // Modern Rose
  // ============================================================

  static const Color rosePrimary = Color(0xFFF43F5E);
  static const Color rosePrimaryDark = Color(0xFFE11D48);
  static const Color rosePrimarySoft = Color(0xFFFFE4E6);

  static const Color roseDarkBackground = Color(0xFF170B10);
  static const Color roseDarkSurface = Color(0xFF21131A);
  static const Color roseDarkVariant = Color(0xFF301B25);

  static const Color roseLightBackground = Color(0xFFFFF6F8);
  static const Color roseLightSurface = Color(0xFFFFFFFF);
  static const Color roseLightVariant = Color(0xFFFCE9EE);

  // ============================================================
  // GOLD
  // Warm Premium Gold
  // ============================================================

  static const Color goldPrimary = Color(0xFFD97706);
  static const Color goldPrimaryDark = Color(0xFFB45309);
  static const Color goldPrimarySoft = Color(0xFFFEF3C7);

  static const Color goldDarkBackground = Color(0xFF15110B);
  static const Color goldDarkSurface = Color(0xFF201A11);
  static const Color goldDarkVariant = Color(0xFF302719);

  static const Color goldLightBackground = Color(0xFFFFFBF4);
  static const Color goldLightSurface = Color(0xFFFFFFFF);
  static const Color goldLightVariant = Color(0xFFF7EFE1);

  // ============================================================
  // MIDNIGHT
  // Deep Indigo / Night Blue
  // ============================================================

  static const Color midnightPrimary = Color(0xFF6366F1);
  static const Color midnightPrimaryDark = Color(0xFF4338CA);
  static const Color midnightPrimarySoft = Color(0xFFE0E7FF);

  static const Color midnightDarkBackground = Color(0xFF090D18);
  static const Color midnightDarkSurface = Color(0xFF12192A);
  static const Color midnightDarkVariant = Color(0xFF1D2840);

  static const Color midnightLightBackground = Color(0xFFF5F7FD);
  static const Color midnightLightSurface = Color(0xFFFFFFFF);
  static const Color midnightLightVariant = Color(0xFFE9ECF8);

  // ============================================================
  // SLATE
  // Cool Blue-Grey
  // ============================================================

  static const Color slatePrimary = Color(0xFF64748B);
  static const Color slatePrimaryDark = Color(0xFF475569);
  static const Color slatePrimarySoft = Color(0xFFE2E8F0);

  static const Color slateDarkBackground = Color(0xFF10151C);
  static const Color slateDarkSurface = Color(0xFF19212B);
  static const Color slateDarkVariant = Color(0xFF26313D);

  static const Color slateLightBackground = Color(0xFFF7F9FB);
  static const Color slateLightSurface = Color(0xFFFFFFFF);
  static const Color slateLightVariant = Color(0xFFE8EDF2);

  // ============================================================
  // TELEGRAM
  // Telegram Sky Blue
  //
  // Main Telegram blue:
  // #2AABEE
  //
  // The surrounding colours are blue-grey/navy neutrals so the
  // bright blue remains the visual focus.
  // ============================================================

  static const Color telegramPrimary = Color(0xFF2AABEE);
  static const Color telegramPrimaryDark = Color(0xFF229ED9);
  static const Color telegramPrimarySoft = Color(0xFFDDF3FC);

  static const Color telegramDarkBackground = Color(0xFF17212B);
  static const Color telegramDarkSurface = Color(0xFF202B36);
  static const Color telegramDarkVariant = Color(0xFF2B3948);

  static const Color telegramLightBackground = Color(0xFFF2F6F9);
  static const Color telegramLightSurface = Color(0xFFFFFFFF);
  static const Color telegramLightVariant = Color(0xFFE7EEF3);

  // ============================================================
  // SIGNAL
  // Clean Communication Blue
  // ============================================================

  static const Color signalPrimary = Color(0xFF3A76F0);
  static const Color signalPrimaryDark = Color(0xFF1B4BBF);
  static const Color signalPrimarySoft = Color(0xFFE8EFFF);

  static const Color signalDarkBackground = Color(0xFF111315);
  static const Color signalDarkSurface = Color(0xFF1B1D20);
  static const Color signalDarkVariant = Color(0xFF292C30);

  static const Color signalLightBackground = Color(0xFFF5F7FA);
  static const Color signalLightSurface = Color(0xFFFFFFFF);
  static const Color signalLightVariant = Color(0xFFE9EDF2);

  // ============================================================
  // DISCORD
  // Discord Blurple
  // ============================================================

  static const Color discordPrimary = Color(0xFF5865F2);
  static const Color discordPrimaryDark = Color(0xFF4752C4);
  static const Color discordPrimarySoft = Color(0xFFE0E3FF);

  static const Color discordDarkBackground = Color(0xFF1E1F22);
  static const Color discordDarkSurface = Color(0xFF2B2D31);
  static const Color discordDarkVariant = Color(0xFF313338);

  static const Color discordLightBackground = Color(0xFFF2F3F5);
  static const Color discordLightSurface = Color(0xFFFFFFFF);
  static const Color discordLightVariant = Color(0xFFE3E5E8);

  // ============================================================
  // TEAL
  // Balanced Teal
  // ============================================================

  static const Color tealPrimary = Color(0xFF009688);
  static const Color tealPrimaryDark = Color(0xFF00796B);
  static const Color tealPrimarySoft = Color(0xFFD8F3F0);

  static const Color tealDarkBackground = Color(0xFF0C1716);
  static const Color tealDarkSurface = Color(0xFF152321);
  static const Color tealDarkVariant = Color(0xFF203430);

  static const Color tealLightBackground = Color(0xFFF2F9F8);
  static const Color tealLightSurface = Color(0xFFFFFFFF);
  static const Color tealLightVariant = Color(0xFFE1F0EE);

  // ============================================================
  // ORANGE
  // Warm Sunset Orange
  // ============================================================

  static const Color orangePrimary = Color(0xFFF97316);
  static const Color orangePrimaryDark = Color(0xFFC2410C);
  static const Color orangePrimarySoft = Color(0xFFFFEDD5);

  static const Color orangeDarkBackground = Color(0xFF17100B);
  static const Color orangeDarkSurface = Color(0xFF211811);
  static const Color orangeDarkVariant = Color(0xFF302219);

  static const Color orangeLightBackground = Color(0xFFFFF8F2);
  static const Color orangeLightSurface = Color(0xFFFFFFFF);
  static const Color orangeLightVariant = Color(0xFFF9E9DB);

  // ============================================================
  // RED
  // Crimson / Warm Charcoal
  // ============================================================

  static const Color redPrimary = Color(0xFFEF4444);
  static const Color redPrimaryDark = Color(0xFFB91C1C);
  static const Color redPrimarySoft = Color(0xFFFEE2E2);

  static const Color redDarkBackground = Color(0xFF170D0E);
  static const Color redDarkSurface = Color(0xFF211617);
  static const Color redDarkVariant = Color(0xFF302021);

  static const Color redLightBackground = Color(0xFFFFF7F7);
  static const Color redLightSurface = Color(0xFFFFFFFF);
  static const Color redLightVariant = Color(0xFFFCEAEA);

  // ============================================================
  // COMMON
  // ============================================================

  static const Color error = Color(0xFFEF4444);

  static const Color success = Color(0xFF22C55E);

  static const Color green = Color(0xFF10B981);

  static const Color earth = Color(0xFF9A3412);

  // ============================================================
  // DARK TEXT
  // ============================================================

  static const Color darkTextPrimary = Color(0xFFF5F7FA);

  static const Color darkTextSecondary = Color(0xFFB6BEC9);

  static const Color darkTextMuted = Color(0xFF7E8996);

  // ============================================================
  // LIGHT TEXT
  // ============================================================

  static const Color lightTextPrimary = Color(0xFF17202A);

  static const Color lightTextSecondary = Color(0xFF52606D);

  static const Color lightTextMuted = Color(0xFF7B8794);

  // ============================================================
  // BORDERS
  // ============================================================

  static const Color darkBorder = Color(0xFF29323C);

  static const Color lightBorder = Color(0xFFE1E6EB);

  static Color parseHexColor(String? hex) {
    if (hex == null || hex.isEmpty) return const Color(0xFF64748B);
    
    try {
      String cleanHex = hex.replaceAll('#', '');
      if (cleanHex.length == 6) {
        cleanHex = 'FF$cleanHex';
      }
      return Color(int.parse(cleanHex, radix: 16));
    } catch (e) {
      return const Color(0xFF64748B);
    }
  }
}